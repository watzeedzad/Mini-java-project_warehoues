CREATE TABLE "ROOT".USERSYS (USER_ID INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 100001, INCREMENT BY 1), USER_NAME VARCHAR (255) NOT NULL, USER_PASSWORD VARCHAR (255) NOT NULL, CONSTRAINT PRIMARY_KEY PRIMARY KEY (USER_ID)); CREATE TABLE "ROOT".PRODUCT (PRODUCT_ID VARCHAR (255) NOT NULL PRIMARY KEY, PRODUCT_NAME VARCHAR (255) NOT NULL); CREATE TABLE "ROOT".STOCK (REF_USER_ID INTEGER NOT NULL, STOCK_PRODUCT_ID VARCHAR (255) NOT NULL, STOCK_PRODUCT_AMOUNT INTEGER NOT NULL, PRIMARY KEY (REF_USER_ID, STOCK_PRODUCT_ID)); ALTER TABLE STOCK ADD FOREIGN KEY (REF_USER_ID) REFERENCES USERSYS (USER_ID); ALTER TABLE STOCK ADD FOREIGN KEY (STOCK_PRODUCT_ID) REFERENCES PRODUCT (PRODUCT_ID); INSERT INTO USERSYS (USER_NAME, USER_PASSWORD) VALUES ('root', '123456');